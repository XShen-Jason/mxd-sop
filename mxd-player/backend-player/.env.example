# start-player.ps1 supplies the local host, port, database, and CORS defaults.
# Generate a 64-character value with: openssl rand -hex 32
PLAYER_SERVICE_TOKEN=replace-with-a-long-random-service-token
